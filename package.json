{
  "name": "terragreen-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static bilingual website for TerraGreen — зелена поддръжка на терени.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "start": "npm run dev",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "engines": {
    "node": ">=20"
  }
}
